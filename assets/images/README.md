# assets/images/

Place any local image assets here.
Currently Pulse News uses remote images from NewsAPI — no local images required to run.

If you'd like to add a custom placeholder image, name it `placeholder.png` and reference it in pubspec.yaml.
